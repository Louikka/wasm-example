# wasm-example

An example of using WebAssembly in the browser (`.wasm` files generated with Golang (`GOOS=js GOARCH=wasm`), served with [Flask](https://pypi.org/project/Flask/)).


## Run this project

```
$ make build
$ flask run
```
